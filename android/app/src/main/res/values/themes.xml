<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
<!--    <style name="Theme.TRTC_Client" parent="Theme.MaterialComponents.DayNight.NoActionBar">-->
    <style name="Theme.TRTC_Client" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/blue_white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Board_Client" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorOnSecondary">@color/blue_white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/blue_white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.TRTC_Client.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.TRTC_Client.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.TRTC_Client.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>